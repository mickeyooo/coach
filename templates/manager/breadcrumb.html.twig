<div class="row wrapper border-bottom white-bg page-heading">
    <div class="col-lg-10">
        <h2>{{ title }}</h2>
        {#<ol class="breadcrumb">
            {% for breadcrumb_item in knp_menu_get_breadcrumbs_array(knp_menu_get_current_item('managerMain')) %}
                {% if not loop.last %}
                    <li class="breadcrumb-item"><a href="{{ breadcrumb_item.uri }}">{{ breadcrumb_item.label }}</a></li>
                {% else %}
                    <li class="breadcrumb-item active">{{ breadcrumb_item.label }}</li>
                {% endif %}
            {% endfor %}
        </ol>#}
    </div>
</div>